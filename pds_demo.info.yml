name: PDS Demo
type: theme
description: "Theme to demo PDS integration."
package: PDS
core_version_requirement: "^8.8 || ^9"

libraries:
  - pds_demo/pds-css
  - pds_demo/local-css
  - pds_demo/pds-js

ckeditor_stylesheets:
  - pds-drupal/css/pds-main.min.css

base theme: stable

regions:
  header: Header
  primary_menu: Primary Menu
  content: Content

component-libraries:
  pds:
    paths:
      - pds-drupal/twig
